# Bug Fixes
1. ~~Login page card is extremely narrow and unusable — the entire card, including inputs, labels, and buttons, is compressed into a very thin column. The layout needs a proper minimum width and the form elements need full-width sizing so the page is actually usable.~~ — **Done (2026-06-01):** Real cause was a Tailwind v4 token collision — the custom `--spacing-md`/`--spacing-sm` tokens in `globals.css @theme` shadow the container scale, so `max-w-md` resolved to 16px (and `max-w-sm` to 12px). The `AuthShell` card was capped at 16px. Fixed with semantic, non-colliding layout-width tokens (`--container-card`/`--container-panel`/`--container-content`) used as `max-w-card`/`max-w-panel`/`max-w-content`; DESIGN.md and the spacing tokens are unchanged. Also fixed the dashboard empty state, chat empty state, and the (12px-wide) mobile nav drawer. Verified via production build. See `decisions.md` (2026-06-01) + `log.md`.

# Major Features to add
