# Bug Fixes


# Major Features to add
1. ~~I want a source tier for social media. Stuff like reddit is useful~~ — **Done (Phase 4+5):** `social_media` added to `source_tier` enum (migration 0009), `SourceTier` TypeScript type, `SourceTierSettings`, create-project form, `createProject` action, `PlanTab` tier labels, and LLM schemas. Search routing to a Reddit/web provider is deferred to Phase 6.