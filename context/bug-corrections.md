# Bug Fixes
1. ~~Login page card is extremely narrow and unusable — the entire card, including inputs, labels, and buttons, is compressed into a very thin column. The layout needs a proper minimum width and the form elements need full-width sizing so the page is actually usable.~~ — **Done (2026-06-01):** Real cause was a Tailwind v4 token collision — the custom `--spacing-md`/`--spacing-sm` tokens in `globals.css @theme` shadow the container scale, so `max-w-md` resolved to 16px (and `max-w-sm` to 12px). The `AuthShell` card was capped at 16px. Fixed by switching the four affected `max-w-{sm,md}` usages to explicit arbitrary widths (`max-w-[28rem]`/`max-w-[24rem]`); also fixed the dashboard empty state, chat empty state, and the (12px-wide) mobile nav drawer. Verified via production build. See `log.md` + `deferredwork.md`.

# Major Features to add
